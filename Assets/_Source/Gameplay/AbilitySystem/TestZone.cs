using UnityEngine;

namespace _Source.Gameplay.AbilitySystem
{
    public class TestZone : MonoBehaviour
    {
        /*private ZoneChooser zoneChooser = new ZoneChooser();

        private void Update()
        {
            if (Input.GetMouseButtonDown(0))
            {
                zoneChooser.GetZone((center,radius) =>
                {
                    GameObject sphere = GameObject.CreatePrimitive(PrimitiveType.Sphere);
                    sphere.transform.position = center;
                    sphere.transform.localScale = new Vector3(radius * 2, 0.1f, radius * 2);
                    Destroy(sphere, 1f);
                    Debug.Log("Центр зоны" + center);
                }, 5f);
            }
        }*/
    }
}